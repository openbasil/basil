# SPDX-FileCopyrightText: 2026 OpenBasil Contributors
#
# SPDX-License-Identifier: Apache-2.0

[package]
name = "basil-nats-bridge"
version.workspace = true
authors.workspace = true
autobins = false
categories = ["network-programming", "command-line-utilities"]
documentation = "https://docs.openbasil.org/clients/nats-bridge/"
edition.workspace = true
homepage.workspace = true
keywords = ["basil", "nats", "grpc", "bridge", "messaging"]
license.workspace = true
publish = true
repository.workspace = true
rust-version = "1.88"
description = "NATS request/reply courier for Basil sealed invocation envelopes."

[package.metadata.dist]
# Keep the bridge in GitHub release archives and shell installers, but do not
# emit a second Homebrew formula from the same workspace release.
installers = ["shell"]

[[bin]]
name = "basil-nats-bridge"
path = "src/main.rs"

[features]
default = []
secure-alloc = ["mimalloc/secure"]

[dependencies]
basil-courier = { workspace = true }
basil-proto = { workspace = true }

anyhow = { workspace = true }
async-nats = { workspace = true, features = ["jetstream", "kv"] }
async-trait = { workspace = true }
base64 = { workspace = true }
bytes = { workspace = true }
clap = { workspace = true }
futures = { workspace = true }
getrandom = { workspace = true }
mimalloc = { workspace = true }
prost = { workspace = true }
serde = { workspace = true, features = ["std"] }
sha2 = { workspace = true }
thiserror = { workspace = true }
tokio = { workspace = true }
toml = { workspace = true }
tonic = { workspace = true }
tracing = { workspace = true }
tracing-subscriber = { workspace = true }

[lints]
workspace = true
